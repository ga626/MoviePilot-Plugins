# S3 增量观察与预览候选回执

状态：`v0.3.5` 本地候选已冻结、已验证；尚未推送、发布、安装或启用。

## 本阶段交付

- 源码候选提交：`2435c39bbeec0f26279af3910465448f4e012686`。
- 候选包：`mediagovernor_v0.3.5.zip`；SHA-256 为 `9582d2fb0262777dc6333ecace7e4b07308bc3fbe7bb7d83ad291395dd86cbd5`。
- 默认关闭。普通启用后只观察之后发生的 `TransferComplete` 与 `TransferFailed`，按通用媒体身份归并为脱敏作品包。
- 既有失败历史回溯改为默认关闭的 `backfill_existing_failures` 开关。只有明确开启才会做一次只读回溯；回溯完成、或宿主不支持该查询合同时，定时服务立即消失且后续重载不会再读取旧历史。
- 继续保留受认证的只读队列查询和固定为 `link + preview` 的预览入口；不移动、改名、删除、重试或创建硬链接。

## 已验证

- 编译检查通过：`python -m compileall -q plugins.v3/mediagovernor tests/v3/mediagovernor`。
- 7 个定向测试函数通过，覆盖增量默认不导入旧历史合同、旧宿主降级、完成后不重查、服务停止、作品包聚合与预览闸门。
- 版本门禁通过：`.github/scripts/check_plugin_versions.py package.v3.json`。
- 差异检查通过：`git diff --check`。
- 候选 ZIP 通过完整性检查，且仅包含 `mediagovernor/` 下的 3 个已跟踪文件。

## 已继承基线与下一关

- 已有的全量历史审计是基线，不在本插件安装时重扫。日常只处理之后发生的新事件。
- 下一关是以同一候选包完成 GitHub PR、云端门禁、合并和 Release，再由 MoviePilot 插件市场安装该正式版本，并保持历史回溯关闭。
- 实际媒体修复属于单独阶段：先按作品包预览，再获得针对硬链接或删除的即时授权；本候选不提供执行入口。

## 阶段回执

- 已编辑：`v0.3.5` 增量默认与本回执。
- 已启动：无；未启动、重建或修改 NAS 容器。
- 已验证：本地确定性检查和候选 ZIP 完整性均通过；真实 V3 加载待正式市场版本安装后验收。
